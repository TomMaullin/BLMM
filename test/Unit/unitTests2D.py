# This file contains all unit tests for the functions given in the tools2D.py
# file.


import sys
# We need to also import tools2D itself, so we first must add lib to the python path.
sys.path.insert(1, 'sys.argv[0]/../../../lib/')

import tools2d.py
